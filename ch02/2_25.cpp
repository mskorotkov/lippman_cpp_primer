int main() {
    int* ip, i, &r = i;
    // 'ip' is a pointer to int
    // 'i' is an integer
    // 'r' is a reference to the integer 'i'

    int i, *ip = 0;
    // 'i' is an integer
    // 'ip' is a pointer to int with NULL value

    int* ip, ip2;
    // 'ip' is a pointer to int
    // 'ip2' is an integer

    return 0;
} 
